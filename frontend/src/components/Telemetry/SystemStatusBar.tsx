/* ─── SystemStatusBar ───
   Minimal placeholder — telemetry UI removed per user request.
   Kept as a no-op export so App.tsx import doesn't break.
*/
export function SystemStatusBar() {
  return null;
}